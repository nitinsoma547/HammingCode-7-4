/**
 * api/ entry point.
 *
 * v1: this file is a placeholder. Once the monthly campaign generator + cron
 * lands, this is where the HTTP routes + scheduled jobs boot.
 */

import { log } from "@/lib/logger";

log.info("api.boot", { msg: "Backend scaffold only — no routes registered yet." });
